# 社交媒体链接配置系统使用说明

## 📋 概述

这是一个基于配置文件的社交媒体链接管理系统,允许你通过简单编辑 JSON 配置文件来管理网站上显示的社交媒体链接,无需手动编辑 HTML 代码。

## 🎯 核心文件

```
tools/
├── social-links.config.json      # 配置文件(你只需要编辑这个)
├── generate-social-links.py      # 生成脚本(自动运行)
└── build.py                      # 构建脚本(已集成生成功能)

sections/links/
└── links.html                    # 自动生成的 HTML(请勿手动编辑!)
```

## 🚀 快速开始

### 1. 修改链接和图标

编辑 `tools/social-links.config.json`:

```json
{
  "links": [
    {
      "name": "LinkedIn",                              // 显示的文字
      "url": "https://linkedin.com/company/yourcompany",  // 链接地址
      "icon": "linkedin",                              // 图标名称
      "enabled": true                                  // 是否显示
    }
  ]
}
```

### 2. 重新构建网站

运行构建命令:

```bash
python3 tools/build.py
```

或使用开发脚本(自动构建):

```bash
./start-dev.sh
```

✅ 完成! 刷新浏览器查看效果。

## 📝 常见操作

### 更改链接地址

只需修改 `url` 字段:

```json
{
  "name": "LinkedIn",
  "url": "https://linkedin.com/company/star-ui",  // 改成你的公司主页
  "icon": "linkedin",
  "enabled": true
}
```

### 更换图标

修改 `icon` 字段,从可用图标列表中选择:

```json
{
  "name": "Twitter",
  "url": "https://twitter.com/yourhandle",
  "icon": "twitter",  // 可选: linkedin, youtube, instagram, tiktok, facebook, twitter, discord, github 等
  "enabled": true
}
```

### 禁用某个链接

将 `enabled` 设为 `false`:

```json
{
  "name": "TikTok",
  "url": "https://tiktok.com/@yourhandle",
  "icon": "tiktok",
  "enabled": false  // 不会显示在网站上
}
```

### 添加新链接

在 `links` 数组中添加新对象:

```json
{
  "links": [
    // ... 现有链接 ...
    {
      "name": "Discord",
      "url": "https://discord.gg/yourinvite",
      "icon": "discord",
      "enabled": true
    }
  ]
}
```

### 调整链接顺序

直接在数组中拖动调整顺序即可,显示顺序与配置文件中的顺序一致。

### 删除链接

有两种方式:

1. **临时隐藏**(推荐): 设置 `"enabled": false`
2. **永久删除**: 直接删除整个对象

## 🎨 可用图标列表

配置文件中 `available_icons` 包含以下图标:

| 图标名称 | 平台 | 示例 URL |
|---------|------|----------|
| `linkedin` | LinkedIn | https://linkedin.com/company/yourcompany |
| `youtube` | YouTube | https://youtube.com/@yourhandle |
| `instagram` | Instagram | https://instagram.com/yourhandle |
| `tiktok` | TikTok | https://tiktok.com/@yourhandle |
| `facebook` | Facebook | https://facebook.com/yourpage |
| `twitter` | Twitter/X | https://twitter.com/yourhandle |
| `discord` | Discord | https://discord.gg/yourinvite |
| `github` | GitHub | https://github.com/yourorg |
| `reddit` | Reddit | https://reddit.com/r/yoursubreddit |
| `spotify` | Spotify | https://open.spotify.com/user/youruser |
| `twitch` | Twitch | https://twitch.tv/yourchannel |
| `pinterest` | Pinterest | https://pinterest.com/yourprofile |

## 🔧 高级配置

### 添加自定义图标

1. 在 [Simple Icons](https://simpleicons.org/) 找到你需要的图标
2. 复制 SVG 的 `<path>` 标签内容
3. 添加到 `available_icons` 对象:

```json
{
  "available_icons": {
    "myplatform": "<path d=\"...你的 SVG path 数据...\"/>"
  }
}
```

4. 在 `links` 中使用:

```json
{
  "name": "My Platform",
  "url": "https://myplatform.com/profile",
  "icon": "myplatform",
  "enabled": true
}
```

### 配置说明字段

`description` 字段仅用于文档说明,不影响生成结果:

```json
{
  "description": "社交媒体链接配置文件 - 修改后运行 python3 tools/build.py 重新生成"
}
```

## ⚠️ 注意事项

1. **不要手动编辑 `sections/links/links.html`**
   - 这个文件是自动生成的
   - 每次构建都会被覆盖
   - 所有修改请在配置文件中进行

2. **修改后必须重新构建**
   - 编辑配置文件后运行 `python3 tools/build.py`
   - 或者使用 `./start-dev.sh`(自动监听变化)

3. **图标名称必须存在于 `available_icons` 中**
   - 使用不存在的图标名称会导致警告
   - 该链接会被跳过

4. **URL 格式要正确**
   - 必须包含 `https://` 或 `http://`
   - 建议使用完整的 URL

## 📖 完整配置示例

```json
{
  "description": "社交媒体链接配置文件",
  "links": [
    {
      "name": "LinkedIn",
      "url": "https://linkedin.com/company/star-ui",
      "icon": "linkedin",
      "enabled": true
    },
    {
      "name": "YouTube",
      "url": "https://youtube.com/@starui",
      "icon": "youtube",
      "enabled": true
    },
    {
      "name": "GitHub",
      "url": "https://github.com/star-ui",
      "icon": "github",
      "enabled": true
    },
    {
      "name": "Discord",
      "url": "https://discord.gg/starui",
      "icon": "discord",
      "enabled": true
    },
    {
      "name": "Twitter",
      "url": "https://twitter.com/starui",
      "icon": "twitter",
      "enabled": false
    }
  ],
  "available_icons": {
    // ... 图标 SVG 数据 ...
  }
}
```

上述配置会显示 4 个链接(LinkedIn, YouTube, GitHub, Discord),Twitter 被禁用不显示。

## 🐛 故障排查

### 问题: 修改配置后没有生效

**解决方案:**
1. 确认已运行 `python3 tools/build.py`
2. 刷新浏览器(Ctrl+F5 强制刷新)
3. 检查配置文件 JSON 格式是否正确(使用 JSON 验证器)

### 问题: 某个图标没有显示

**解决方案:**
1. 检查 `icon` 名称是否在 `available_icons` 中存在
2. 查看构建输出是否有警告信息
3. 确认 `enabled` 设置为 `true`

### 问题: 链接顺序不对

**解决方案:**
- 链接显示顺序与配置文件中的顺序一致
- 在 `links` 数组中调整对象的顺序
- 重新构建

### 问题: 构建失败

**解决方案:**
1. 检查 JSON 格式是否正确(逗号、引号、括号)
2. 使用 JSON Lint 验证: https://jsonlint.com/
3. 查看错误信息,通常会指出具体问题

## 💡 最佳实践

1. **保持启用的链接数量在 3-6 个**
   - 太多会显得杂乱
   - 太少可能浪费空间

2. **使用常见平台图标**
   - 用户更熟悉
   - 识别度更高

3. **确保 URL 有效**
   - 定期检查链接是否有效
   - 避免 404 错误

4. **使用 `enabled` 而不是删除**
   - 临时禁用使用 `"enabled": false`
   - 方便以后重新启用

5. **备份配置文件**
   - 修改前先备份
   - 使用 Git 管理变更

## 📚 相关文档

- [Simple Icons](https://simpleicons.org/) - 2000+ 品牌 SVG 图标
- [JSON Lint](https://jsonlint.com/) - JSON 格式验证工具
- [Star-UI 主文档](../CLAUDE.md) - 项目完整文档

## 🆘 需要帮助?

如果遇到问题:

1. 查看构建输出的错误信息
2. 检查配置文件 JSON 格式
3. 参考本文档的故障排查部分
4. 查看 Git 提交历史,对比工作版本

---

**最后更新**: 2025-11-03
**版本**: 1.0.0
